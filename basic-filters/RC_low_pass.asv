clc;clear all;close all
R1= 1e3 ;
C1= 1e-6;
num = [1/(R1*C1)] ;
denom=[1,1/(R1*C1)];
sys = tf(num,denom);
figure
opts = bodeoptions;
 
opts.Title.FontSize = 15;
opts.Title.Color = 'blue';
opts.Grid = 'on';
opts.FreqScale = 'log';
opts.Title.String = 'Bode Plot of RC Low Pass Filter For R = 1kOhm C = 1';
h = bodeplot(sys,opts);
 

figure
pzplot(sys)